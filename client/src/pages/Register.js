import React from 'react';
import styled from 'styled-components';
import { NavLink } from 'react-router-dom';

const RegisterBlock = styled.div``;

function Register() {
  return <RegisterBlock>회원가입 페이지</RegisterBlock>;
}

export default Register;
